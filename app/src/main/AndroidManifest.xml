<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StateCompose"
        tools:targetApi="31">
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.StateCompose">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.WellnessActivity"
            android:exported="true"
            android:theme="@style/Theme.StateCompose" />
        <activity
            android:name=".activity.LaunchedEffectActivity"
            android:exported="true"
            android:theme="@style/Theme.StateCompose" />
        <activity
            android:name=".activity.BottomMenuPanelActivity"
            android:exported="true"
            android:theme="@style/Theme.StateCompose"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activity.AccordionActivity"
            android:exported="true"
            android:theme="@style/Theme.StateCompose" />
        <activity android:name=".activity.BasicLayoutActivity"
            android:exported="true"
            android:screenOrientation="unspecified"
            android:theme="@style/Theme.StateCompose" />
        <activity android:name=".activity.AnimateShapeActivity"
            android:exported="true"
            android:theme="@style/Theme.StateCompose" />
    </application>

</manifest>